



<!--div class="container">
<div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
  <h1 class="display-5">Bem vindo(a)</h1>
  <p class="lead">Segue as principais opções disponíveis para você.</p>
</div>
  <div class="card-deck mb-3 text-center">
    <div class="card mb-4 box-shadow">
      <div class="card-header">
        <h4 class="my-0 font-weight-normal">Solicitar exame</h4>
      </div>
      <div class="card-body">
        <p class="lead">Utilize essa opção se você deseja solicitar um novo exame.</p>
        <%= link_to('Solicitar novo exame', new_exame_requisicao_path, class: "btn btn-lg btn-block btn-primary") %>
      </div>
    </div>
    <div class="card mb-4 box-shadow">
      <div class="card-header">
        <h4 class="my-0 font-weight-normal">Resultados</h4>
      </div>
      <div class="card-body">
        <p class="lead">Utilize essa opção para verificar os resultados ou acompanhar os status dos exames solicitados previamente.</p>
        <%= link_to('Resultados', exame_requisicoes_path, class: "btn btn-lg btn-block btn-primary") %>
      </div>
    </div>
  </div>
</div -->




<!--%= image_tag curent_user.gravatar_url %-->
<div class="container">


<div class="jumbotron">
  <h1 class="display-4"><%= @lab.nome %></h1>
  <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
  <p class="lead">
    <% if current_user.nil? %>
    <hr class="m-y-2">
    <p>Na primeira solicitação você precisará registra-se no sistema.</p>
    <% end %>
    <%= link_to('Solicitar exame »', new_exame_requisicao_path, class: "btn btn-primary btn-lg") %>
  </p>
</div>

  <h1 id="tipos">Exames disponíveis</h1>
  <ul class="list-group list-group-flush">
    <% @lab.exame_tipos.each do |tipo| %>
    <li class="list-group-item"><%= tipo.nome %></li>
    <% end %>
  </ul>
</div>